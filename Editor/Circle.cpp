#include "Circle.h"

double PI = 3, 14;

int Circle::getRadius() {
	return radius;
}
int Circle::getRadius() {
	return radius;
}

void Circle::setRadius() {
	this->radius = radius;
}
void Circle::setRadius() {
	this->radius = radius;
}

double Circle::perimetr(double radius){
	return 2 * PI * radius;
}
double Circle::square(double radius) {
	return PI * radius * radius;
}
string Circle::draw() {
	string msg = "Circle int x = " + to_string(x) + ",y=" + to_string(y);
}